# SimplePrint Language

SimplePrint is a simple programming language that prints text to the console.

## Building

Run the build script to compile SimplePrint and prepare the installation script:

```bash
./build.sh
```
